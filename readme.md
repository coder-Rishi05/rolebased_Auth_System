# 🔐 AUTH + ROLE BASED ACCESS CONTROL (RBAC)

- Auth Controllers → register & login

- JWT Verification Middleware → verifyToken

- Role Authorization Middleware + Routes → authorizeRole + protected routes